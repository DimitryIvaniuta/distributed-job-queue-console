import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { jobFixture } from '../../test/fixtures'; import { JobDetails } from './JobDetails';
describe('JobDetails',()=>{it('renders durable job state safely',()=>{render(<JobDetails job={{...jobFixture,status:'SUCCEEDED',resultJson:'{"settled":true}'}}/>);expect(screen.getByText(jobFixture.id)).toBeInTheDocument();expect(screen.getByText('SUCCEEDED')).toBeInTheDocument();expect(screen.getByText('BANK_TRANSFER_SETTLEMENT')).toBeInTheDocument();expect(screen.getByText(/settled/)).toBeInTheDocument();});});
